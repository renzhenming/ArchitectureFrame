<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rzm.commonlibrary">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <service
            android:name="com.rzm.commonlibrary.general.guard.GuardService1"
            android:enabled="true"
            android:exported="true"
            android:process=":guardService1" />
        <service
            android:name="com.rzm.commonlibrary.general.guard.GuardService2"
            android:enabled="true"
            android:exported="true"
            android:process=":guardService2" />
        <service
            android:name="com.rzm.commonlibrary.general.guard.JobWakeUpService1"
            android:enabled="true"
            android:exported="true"
            android:process=":guardService1"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <service
            android:name="com.rzm.commonlibrary.general.guard.JobWakeUpService2"
            android:enabled="true"
            android:exported="true"
            android:process=":guardService2"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <!--   android:finishOnTaskLaunch="true"在配置了该属性为true的activity中按home键返回到[home screen]屏幕后,再点击该应用的图标启动程序时,
            则系统会调用该activity的[onDestroy]销毁。因为点击应用的图标启动程序时,重新启动了这个任务。
            excludeFromRecents 是否在最近打开的应用程序中显示

            -->
        <activity android:name="com.rzm.commonlibrary.general.guard.DaemonActivity"
            android:configChanges="keyboardHidden|orientation|screenSize|navigation|keyboard"
            android:excludeFromRecents="true"
            android:exported="false"
            android:finishOnTaskLaunch="false"
            android:launchMode="singleInstance"
            android:theme="@style/DaemonActivityStyle"/>

    </application>

</manifest>
